# @dhcw/sr-web

Canonical HTML/CSS components for the DHCW Single Record Design System.

Framework-agnostic reference implementation following the GOV.UK Frontend model. Blazor and React packages wrap these components.

**Status:** Planned — component specs are in `/components/`, implementation to follow.
