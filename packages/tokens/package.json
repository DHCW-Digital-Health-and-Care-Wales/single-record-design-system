{
  "name": "@dhcw/sr-tokens",
  "version": "0.1.0",
  "description": "DHCW Single Record Design System — design tokens. Outputs CSS custom properties, SCSS variables, and XAML ResourceDictionary from the canonical JSON token source.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "style-dictionary": "^5.5.0"
  },
  "files": [
    "build/"
  ]
}
