# @dhcw/sr-icons

SVG icon library for the DHCW Single Record Design System.

Source icons live in `/foundations/iconography/svg/`. This package will provide an SVG sprite and per-framework icon components (React, Blazor).

**Status:** Planned — icons are currently consumed directly from `/foundations/iconography/svg/`.
